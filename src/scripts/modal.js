import { log } from "util";

// export default function triggerModal() {
    // document.addEventListener('DOMContentLoaded', () => {
        // let btn = document.querySelector('.container__btn');
        // let modal = document.querySelector('.modal');

        // btn.addEventListener('click', () => {
        //     modal.classList.add('modal--open');
        //     console.log();
            
        // });

        // let btnClose = document.querySelector('.close__btn');
        // let block = document.querySelector('[data-end]');

        // let endAnimation = () => {
        //     block.classList.remove('fadeOutDown');
        //     modal.classList.remove('modal--open');
        //     block.removeEventListener('animationend', endAnimation);
        // };

        // btnClose.addEventListener('click', () => {
        //     block.addEventListener('animationend', endAnimation);
        //     block.classList.add(block.getAttribute('data-end'));
        // });
    // });
// };
